{
  "name": "testnova",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "build": "npm run build --workspace client -- --outDir ../dist && echo 'Build completed'",
    "build:client": "npm run build --workspace client",
    "dev:client": "npm run dev --workspace client",
    "dev:server": "npm run dev --workspace server"
  }
}
